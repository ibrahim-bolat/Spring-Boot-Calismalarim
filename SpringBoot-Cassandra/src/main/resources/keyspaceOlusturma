create keyspace ibo
    WITH replication = {'class':'SimpleStrategy', 'replication_factor' : 3};
use ibo;
use ibo;

CREATE TABLE kisi(
       id int PRIMARY KEY,
       ad text,
       soyad text,
       pricae int,
       adres varchar,
       dogumTarihi timestamp
      );

 select * from kisi;